<%= provide(:title, "#{@user.first_name} #{@user.second_name}") %>
<div class="row">
  <aside class="span4">
    <section>
      <div id="avatar">
        <%= avatar_for(@user, :thumb) %>
      </div>
      <h1>
        <%= @user.first_name %> <%= @user.second_name %>
      </h1>
    </section>
  </aside>
</div>
<%= @user.email %>
