<% provide(:title, 'Все пользователи') %>
<h1>Все пользователи</h1>

<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>

<% if current_user.admin? %>
    <%= link_to "Добавить пользователя", signup_path, class: "btn btn-large btn-primary"%>
<% end %>